# Reserve only the USB gadget ALSA card for the Siri microphone bridge.
monitor.alsa.rules = [
  {
    matches = [ { device.name = "alsa_card.platform-1000480000.usb" } ]
    actions = { update-props = { device.disabled = true } }
  }
]
